info:
  name: PUT - Substituir completo
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/cursos/1"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "id": 1,
        "descricao": "Ciencia da Computacao Atualizado"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5