info:
  name: GET - Listar todos
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/cursos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5