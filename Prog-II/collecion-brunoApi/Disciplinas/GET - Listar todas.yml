info:
  name: GET - Listar todas
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/disciplinas"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
