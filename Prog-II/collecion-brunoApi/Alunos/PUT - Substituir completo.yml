info:
  name: PUT - Substituir completo
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseUrl}}/api/alunos/2"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "nome": "Bruno lima atualizado",
        "matricula": "2024123",
        "id_curso": 1,
        "ativo": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
