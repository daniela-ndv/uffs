info:
  name: GET - Listar todos completo
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/alunos?completo=true"
  params:
    - name: completo
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
