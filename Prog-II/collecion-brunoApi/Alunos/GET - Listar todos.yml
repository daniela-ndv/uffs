info:
  name: GET - Listar todos
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/alunos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
