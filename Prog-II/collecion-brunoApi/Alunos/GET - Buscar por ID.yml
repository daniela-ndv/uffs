info:
  name: GET - Buscar por ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/alunos/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
