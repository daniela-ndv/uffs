info:
  name: POST - Criar novo
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/alunos"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "nome": "Ciclano de tal",
        "matricula": "2024111",
        "curso": "CC",
        "id_curso": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
