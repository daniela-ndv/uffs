info:
  name: PATCH - Atualizar parcial
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseUrl}}/api/alunos/1"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "curso": "SI"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
