info:
  name: PATCH - Atualizar parcial
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/api/matriculas/1"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "nota": 7.5,
        "situacao": "CURSANDO"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5