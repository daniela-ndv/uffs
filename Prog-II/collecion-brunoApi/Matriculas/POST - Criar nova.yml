info:
  name: POST - Criar nova
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/matriculas"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "id_aluno": 1,
        "id_curso": 1,
        "semestre": "2026-1",
        "nota": 8.5,
        "situacao": "CURSANDO"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5